# Changelog

The canonical, release-managed changelog for the published `kremory` crate lives at:

## → [`crates/kremory/CHANGELOG.md`](crates/kremory/CHANGELOG.md)

That file ships to crates.io and is maintained by release-please
(`release-please-config.json`). It covers `0.3.0` → current.

**Pre-0.3.0 history** (`0.1.0` → `0.2.0`) previously lived in this root file and
remains fully available in git history:

```
git show 3aad402:CHANGELOG.md   # last root-changelog revision before this redirect
```

This root file was a pre-split duplicate that had drifted (it stopped at `0.3.0`
while the crate changelog advanced to `0.3.2` → `0.4.0`); it is now a redirect so
there is a single source of truth. See
`.ai-docs/release-please-setup-notes-2026-07-12.md`.
